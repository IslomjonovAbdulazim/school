import 'package:flutter/material.dart';

class TAddMedia extends StatelessWidget {
  const TAddMedia({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}

