import 'package:flutter/material.dart';

class GWSettingsPage extends StatelessWidget {
  const GWSettingsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
